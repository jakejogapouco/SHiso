# SHISO — Compacted ISO Extractor

A simple Bash script to extract Xbox 360 ISOs on Linux using `extract-xiso`, `zenity`, and `pv`.  
It provides a minimal graphical interface to select multiple ISOs, choose a destination folder, and display extraction progress.

---

## ⚙️ Requirements

Make sure these packages are installed:

bash
sudo apt install zenity pv
You also need extract-xiso, available here:
👉 https://github.com/XboxDev/extract-xiso

💡 Usage
Make the script executable:

Right-click the file → Properties → Permissions → Allow executing file as a program

Or run this command in Ubuntu or any Ubuntu-based distro:

bash
Copiar código
chmod +x /path/to/shiso_extract.sh
To run it:

Double-click the file (if set as executable), or

Run it manually:

bash

./shiso_extract.sh
(If needed, use sudo ./shiso_extract.sh)

📦 Features
Native Linux (no Wine required)

Extract multiple Xbox 360 ISOs at once

Zenity-based GUI — lightweight and simple

Progress bar using pv

Automatically removes partially extracted folders if cancelled

⚠️ Disclaimer
This script was generated by ChatGPT (OpenAI) based on user requests.
I’m not a Python or Bash developer, and I don’t provide bug fixes or support.
Use it at your own risk — it’s open source and meant to help other Linux users
who have trouble extracting Xbox 360 ISOs.

📜 License
MIT License — feel free to modify and share.
